

1、malfunction.setUpdateBy("0");//写死？

2、==========================================
DeviceExample example = new DeviceExample();
example.createCriteria().andDeviceIdentityEqualTo(malfunction.getDeviceIdentity());
deviceMapper.updateByExampleSelective(record, example);

==========================================，两者区别
baseMapper.updateByPrimaryKey(malfunction);

3、判断能否用押金置换代码重复 @RequestMapping("/payEquityCard")