	public static AppResultObj serverError(String msg) {
		return new AppResultObj(AppResultObj.CODE_SERVER_ERR, msg, null, false);
	}
	
	public static AppResultObj parameterError(String msg) {
//		return new AppResultObj(AppResultObj.CODE_PARAM_ERR, "参数解析错误", msg, false);
		
		//2017-07-03  返回的错误信息从data字段替换msg字段
		return new AppResultObj(AppResultObj.CODE_PARAM_ERR, msg, null, false);
	}
	
@RequestMapping("/start")【ArController.start】

推送：服务接口？ARService


sendARCourseRecordTOLifeHouseUrl: http://betamanagelife2.healthmall.cn/gateway/sail/api/sail/ar/saveArCourseData

	//update by ljt
	@Value("${httpclient.devMgmt.sendARCourseRecordTOLifeHouseUrl}")
	private String arForLifeUrlHouseUrl;
	/**
	 * 推送到馆务数据
	 * @param arRecord AR课程记录信息
	 * @return
	 */
	public AppResultObj pushToLifeHouse(ArRecord arRecord) {
		HttpHeaders headers = new HttpHeaders();
		MediaType type = MediaType.parseMediaType("application/json; charset=UTF-8");
		headers.setContentType(type);
		String jsonData = JsonUtils.toJson(arRecord);
		HttpEntity<String> requestEntity = new HttpEntity<String>(jsonData, headers);
		
		AppResultObj result = restTemplate.postForObject(accessService.getSendARCourseRecordTOLifeHouseUrl(arRecord.getDeviceId()), requestEntity,
				AppResultObj.class);
		return result;
	}

	
	

base_ar_course
 `name` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '课程名称',
 `trainer` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '授课老师',
 
mem_ar_record_detail
`score` decimal(10,1) DEFAULT NULL COMMENT '运动得分成绩',
	
可以查对应场馆 
dev_gym
`device_id` varchar(32) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '设备id',

=========Ar课程信息====6张表中查询=========
base_ar_course
mem_ar_record
dev_device
mem_member
mem_ar_record_detail
dev_gym
======================

	
	主要已知参数
	arInDTO.getDeviceId(), memDeviceRel.getMemId()
	
	通过已知参数获取到的数据：
	private BigDecimal achievement;//总成绩
	private BigDecimal energyConsumption;//能耗 卡
	private Integer finishCourse;//是否完成视频 0否 1是
	private String endTime;//结束时间
	private String startTime;//开始时间
	private BigDecimal sportDuration;//运动时长
	private Integer status;//'0、初始化，1、ar课程启动ok，2、ar课程结束中，3，ar课程结束Ok'
	private String courseId;//课程id
	
	数据库中获取
	private String trainer;//授课老师  教练
	private String courseName;//课程名称
	
	private BigDecimal score;//运动得分
	
	还未查询加入的数据
	private Integer lifeNo;//生活馆编号
	private String mallId;//用户猫号
	
https://beta-sailweb.healthmall.cn/api/questionResult/add
--------------推送到馆务的数据-----------
{"lifeNo":887,"mallId":"t2042484636","trainer":"孙敏","courseName":"肠胃调理八法","score":0.0,"achievement":17.2,"energyConsumption":1.60,"finishCourse":0,"deleteFlag":0,"endTime":"2017-10-16 15:45:55","startTime":"2017-10-16 15:45:42","sportDuration":1,"status":3,"courseId":"4"}


	
	
	
	