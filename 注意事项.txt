
1、查询数据过滤去删除标记的数据：where delete_flag = 0  【0：未删除  1：已删除】

2、if判断时需进行判空处理。

3、mem_extend_info 与 mem_member id完全一样

4、Assert.notNull(memSportReport, "系统没有该体测报告");  --> 类似于判空处理

5、BeanUtils.copyProperties(tmp, outDTO);将tmp里边的值copy到outDTO中。